
on:
  push:
    branches:
      - PE-test
      
    
jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - name: Echo github variables
        id: get_ref
        run: |
          echo "brokers-dmx-prod-non-revenue-eks-2.kafka-us-east-1b.vungle.io 66\nbrokers-dmx-prod-non-revenue-eks-3.kafka-us-east-1b.vungle.io 78" >> final_result
          echo "content=$(cat final_result)" >> $GITHUB_OUTPUT
          
      # To be continue for run ANsible script  
      - name: Run Ansible playbook
        run: |
          echo "${{ steps.get_ref.outputs.content }}"
       